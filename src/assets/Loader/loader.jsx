// src/components/Loader.jsx
import React from 'react';
import '../../Navbar/App.scss'; // Ensure the CSS for your loader is properly imported

const Loader = () => {
  return (
    <div className="loader">
      {/* Loader styling */}
    </div>
  );
};

export default Loader;
